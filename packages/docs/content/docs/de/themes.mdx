---
title: Themes
description: Passe ShipChamber mit eingebauten und benutzerdefinierten Themes an.
---

# Themes

ShipChamber unterstützt eingebaute Themes und benutzerdefinierte Theme-JSON-Dateien.

## Ein benutzerdefiniertes Theme hinzufügen

1. Erstelle das Themes-Verzeichnis:

```bash
mkdir -p ~/.config/shipchamber/themes
```

2. Füge deine JSON-Datei in dieses Verzeichnis ein (zum Beispiel `my-theme.json`).
3. Öffne ShipChamber und gehe dann zu **Einstellungen -> Theme -> Themes neu laden**.
4. Wähle dein Theme im Dropdown aus. Es wird sofort angewendet.

## Speicherort der Themes

- macOS/Linux: `~/.config/shipchamber/themes/`

## Vollständige Referenz des JSON-Formats

Verwende die vollständige Formatbeschreibung in den Haupt-Repository-Dokumenten:

- [`docs/CUSTOM_THEMES.md`](https://github.com/bytepassperks/shipchamber/blob/main/docs/CUSTOM_THEMES.md)
